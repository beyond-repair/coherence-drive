# Audit 2026-09-12 — Coherence Drive epistemic lock

**Classification:** RESEARCH  
**Claim level:** ≤ 2 (framework / phenomenology)  
**Does not raise Stage 1 freeze. Does not claim thrust.**

## End result

The framework is mathematically more coherent than the 2026-04 cluster READMEs.  
The central physical claim (anomalous propulsion / validated $W$-thrust) is **not** validated.

| Layer | Status |
|-------|--------|
| Action / Noether $T_{\rm eff}$ | Structurally defensible. Implementation-level $\nabla_\mu T^{\mu\nu}_{\rm eff}=0$ on a mesh is **not** verified in this repo. |
| ADCE | Legitimate **photon** momentum reservoir: $F\approx P_{\rm asym}/c$. This is a radiating converter, not a reactionless drive. |
| M2 ratios | Parameter-free product model gives $\approx 0.589:1:1.699$. $W$-only gives $\approx 0.795:1:1.259$. Hybrid $0.795:1:1.993$ is **rejected**. |
| $\chi,\eta,D_{\rm eff}(n)$ patches | Calibration. Forbidden as a path to the hybrid. |
| Biological / PIF / Orch-OR | Speculative, unquantified. Not part of the hard core. |

**Overall:** coherent, falsifiable effective-theory / modeling framework with an unresolved empirical prediction. Not a demonstrated new propulsion or biological phenomenon.

## Canonical M2 (do not silently switch indexing)

$$
W(n)=0.08\,e^{0.23(n-3)}
$$

Numeric lock and reproduction live in the satellite:

- [m2-renormalization-law](https://github.com/beyond-repair/m2-renormalization-law)
- [FALSIFICATION.md](https://github.com/beyond-repair/m2-renormalization-law/blob/main/FALSIFICATION.md)
- `python3 scripts/parameter_free_sweep.py`

## Decisive next test (Stage 2 only)

Fixed geometry: 0.45 asymmetric Sierpiński tetrahedron.  
**Zero** fitted $n$-dependent parameters.

$$
G_n \rightarrow \mathrm{LDOS}_n \rightarrow F_n
$$

If the chain reproduces a published ratio set, that set may be promoted.  
If not, keep Model A or Model B and leave the hybrid rejected.

$\kappa$ / $\chi_{\rm vac}$ must not be chosen to hit $3\times 10^{-8}\,\mathrm{N/W}$. That target is a design goal, not a fit anchor.

## Immutable flags

```text
experimental_validation     = false
thrust_validated            = false
energy_extraction_validated = false
hybrid_m2_ratios_0.795_1.993 = rejected
compensator_chi_eta         = forbidden
```
