# Fix log — 2026-09-22

**Stage 1 freeze was not unlocked.** The symbolic chain in
`MATH_THEORY_CLOSURE.md` is not what was broken.

## What was fixed

1. `stress-tensor-modification/physics_evaluator.py`  
   `model="star"` now defaults to \(W_\star=1/(4\pi)\).  
   `model="M2"` still pins \(W(3)=0.08\) with the frozen exponent.

2. `ware-constant-phenomenology.md`  
   Removed deprecated \(n-1\) indexing, hybrid 1.993, and thrust-as-calibration.
   File is now a historical note plus the lock.

## What was not unlocked

- \(\Delta F=W\chi\mathcal{G}\) symbolic chain
- Claim flags (all remain false)
- \(c_\star=1\) as a theorem
- Hybrid M2 ratios
- Stage 2

GitHub *repository descriptions* that still say thrust is implemented were not
editable with the connected file API this pass.
